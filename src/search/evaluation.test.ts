import { describe,expect,it } from 'vitest';import type { Query } from '../types';import { FIXTURE_CORPUS } from './corpus';import { search } from './engine';
const transform=(p:number[],transpose=0,scale=1):Query=>({version:1,mode:'melody_rhythm',meter:'4/4',startsOnDownbeat:true,events:p.map((x,i)=>({id:`e${i}`,kind:'note',pitchMidi:x+transpose,durationRatio:((i===1||i===2)?.5:1)*scale}))});
describe('synthetic evaluation',()=>{it('recalls a transposed and tempo-scaled phrase at rank 1',()=>{const r=search(transform([60,62,64,67],5,2),FIXTURE_CORPUS);expect([...r.exact,...r.similar][0].work.workId).toBe('fixture-rhythm')});it('penalizes a reversed contour',()=>{const good=search(transform([60,62,64,67]),FIXTURE_CORPUS).similar[0]?.localSimilarity??100;const bad=search(transform([67,64,62,60]),FIXTURE_CORPUS).similar[0]?.localSimilarity??0;expect(good).toBeGreaterThan(bad)});});
