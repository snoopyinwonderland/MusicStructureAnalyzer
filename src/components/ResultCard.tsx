import{useMemo,useRef,useState}from'react';import{ChevronDown,ExternalLink,Pause,Play,Sparkles}from'lucide-react';import type{MatchResult}from'../types';import{withMeasureRests}from'../music/display';import{SearchExcerpt}from'./SearchExcerpt';const Score=({label,value}:{label:string;value:number})=><div className="score-row"><span>{label}</span><div><i style={{width:`${value}%`}}/></div><b>{Math.round(value)}</b></div>;
export function ResultCard({result,index}:{result:MatchResult;index:number}){const{work}=result,[playing,setPlaying]=useState(false),timers=useRef<number[]>([]),targets=useMemo(()=>result.alignment.filter(a=>a.candidateIndex!==null&&a.type!=='insertion').map(a=>work.notes[a.candidateIndex!]).filter(Boolean),[result,work]),events=useMemo(()=>withMeasureRests(work.notes),[work]);const play=()=>{timers.current.forEach(clearTimeout);if(playing){setPlaying(false);return}setPlaying(true);let at=0;for(const n of events){const ms=625*n.durationRatio;if(n.kind==='note')timers.current.push(setTimeout(()=>{const c=new AudioContext(),o=c.createOscillator(),g=c.createGain();o.frequency.value=440*2**((n.pitchMidi!-69)/12);g.gain.setValueAtTime(.06,c.currentTime);g.gain.exponentialRampToValueAtTime(.001,c.currentTime+Math.min(.55,ms/1000));o.connect(g).connect(c.destination);o.start();o.stop(c.currentTime+Math.min(.6,ms/1000))},at));at+=ms}timers.current.push(setTimeout(()=>setPlaying(false),at))};const url=`/score?workId=${encodeURIComponent(work.workId)}&start=${result.startMeasure}&end=${result.endMeasure}`;return<article className="result-card expanded" style={{'--accent':work.accent}as React.CSSProperties}><div className="result-rank">{String(index+1).padStart(2,'0')}</div><div className="album-mark"><span>MN</span></div><div className="result-main"><div className="eyebrow"><span className={`badge ${result.kind}`}>{result.kind==='exact'?'Exact':'Similar'}</span><span>{work.genre}</span></div><h3>{work.title}</h3>{work.artist&&<p className="artist">{work.artist}</p>}<div className="match-meta"><span>마디 {result.startMeasure} · {result.startBeat}박–{result.endMeasure}</span><span>{work.partName||work.streamId}</span></div><details><summary><Sparkles size={14}/> Why this matched <ChevronDown size={14}/></summary>{result.why.map(x=><p key={x}>{x}</p>)}</details></div><div className="result-scores"><div className="big-score"><span>{result.kind==='exact'?'Exact match':'Similarity'}</span><strong>{Math.round(result.localSimilarity)}</strong></div><Score label="Pitch" value={result.scores.pitch}/><Score label="Interval" value={result.scores.interval}/><Score label="Contour" value={result.scores.contour}/><Score label="Rhythm" value={result.scores.rhythm}/><div className="confidence">Occurrence importance {Math.round(result.occurrenceImportance)}</div></div><div className="result-actions"><button aria-label="악보 조각 재생" onClick={play}>{playing?<Pause size={16}/>:<Play size={16}/>}</button>{work.youtubeId&&<a href={`https://youtu.be/${work.youtubeId}`} target="_blank" rel="noreferrer"><ExternalLink size={16}/></a>}</div><div className="result-notation"><div><b>{work.partName||work.streamId} · 원본 악보 조각</b><span>붉은 음표가 검색된 구간</span></div><div className="result-media"><a className="score-link" href={url}><SearchExcerpt workId={work.workId} start={result.startMeasure} end={result.endMeasure} targets={targets}/></a>{work.youtubeId&&<iframe loading="lazy" src={`https://www.youtube-nocookie.com/embed/${work.youtubeId}`} title={`${work.title} 대표 영상`} allowFullScreen/>}</div></div></article>}
