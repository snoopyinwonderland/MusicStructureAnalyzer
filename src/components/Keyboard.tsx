import { isBlack, pitchName } from '../music/pitch';
export function Keyboard({onNote,preferFlat,onPreferenceChange}:{onNote:(m:number)=>void;preferFlat:boolean;onPreferenceChange:(flat:boolean)=>void}){const keys=Array.from({length:53},(_,i)=>36+i);return <div className="keyboard" role="group" aria-label="C2부터 E6까지의 멜로디 건반"><div className="accidental-toggle" aria-label="검은 건반 임시표 선택"><button className={!preferFlat?'active':''} onClick={()=>onPreferenceChange(false)}>♯</button><button className={preferFlat?'active':''} onClick={()=>onPreferenceChange(true)}>♭</button></div><div className="keyboard-keys">{keys.filter(k=>!isBlack(k)).map(k=><div key={k} role="button" tabIndex={0} className="white-key" onClick={()=>onNote(k)} onKeyDown={e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();onNote(k)}}} aria-label={pitchName(k,preferFlat)}><span>{k%12===0?pitchName(k):''}</span>{keys.includes(k+1)&&isBlack(k+1)&&<button className="black-key" aria-label={pitchName(k+1,preferFlat)} onClick={e=>{e.stopPropagation();onNote(k+1)}}/>}</div>)}</div></div>}
