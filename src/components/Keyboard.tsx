import { isBlack, pitchName } from '../music/pitch';
export function Keyboard({onNote}:{onNote:(m:number)=>void}){const keys=Array.from({length:53},(_,i)=>36+i);return <div className="keyboard" role="group" aria-label="C2부터 E6까지의 멜로디 건반"><div className="keyboard-keys">{keys.filter(k=>!isBlack(k)).map(k=><div key={k} role="button" tabIndex={0} className="white-key" onClick={()=>onNote(k)} onKeyDown={e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();onNote(k)}}} aria-label={pitchName(k)}><span>{k%12===0||k===88?pitchName(k):''}</span>{keys.includes(k+1)&&isBlack(k+1)&&<button className="black-key" aria-label={pitchName(k+1)} onClick={e=>{e.stopPropagation();onNote(k+1)}}/>}</div>)}</div></div>}
